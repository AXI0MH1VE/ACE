[package]
name = "axiomhive-app"
version = "0.1.0"
description = "Tauri desktop UI for AxiomHive."
edition = "2021"

[dependencies]
tauri = { version = "1", features = ["dialog-all", "shell-open"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
axiomhive = { path = ".." }
